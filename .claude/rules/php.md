---
paths:
  - "php/**/*.php"
  - "php/composer.json"
  - "php/phpunit.xml"
---

# PHP Rules

## Tests

- Test classes extend `FixtureTestCase`, use `fixturesAsProvider()` for data-driven tests.
- Fuzz tests use `mb_str_split` on a character pool — NOT `random_bytes` (ext-php-rs rejects invalid UTF-8).
- No `composer.lock` committed. CI uses `composer update` to resolve per PHP version.

## Fixtures

- Generated by `php/fixture-generator/generate.php` inside the profiling Docker stack.
- Per-function definitions in `php/fixture-generator/functions/<name>.php`.
- Output to `fixtures/<name>.json` — shared by Rust and PHP tests.

## Bridge

- `php/bridge/patina-bridge.php` calls `patina_activate()` as a mu-plugin.
- Must check `extension_loaded('patina-ext')` and respect `PATINA_DISABLE`.

## Benchmarks

- Reference WordPress implementations (verbatim copies) in `php/benchmarks/reference/`.
- Harness in `Runner.php` reports per-function speedup.
